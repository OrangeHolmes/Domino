import java.util.Random;
public class Bateador {
    
  private Jugador bateador;
  private Ficha[] misFichas;
  private Ficha miFicha;
 
  public Bateador(Jugador nuevo, Ficha[] mias){
    bateador= nuevo; 
    misFichas = mias;
    
    }
  
 public String noSeJugar(int posicion, String ladoJ, Ficha[] fichasJ){
   Random aleatorio = new Random();
   String miParteDeLaMesa=null;
   this.misFichas = fichasJ;
   boolean puedoSeguir= false;
   int ladoI=0;
   int ladoD=0;
   while(puedoSeguir == false){
    int n = aleatorio.nextInt(7)+1;
    this.miFicha = miFicha.escogerFicha(fichasJ,n);
    puedoSeguir= miFicha.esValida(miFicha);}
   ladoI= miFicha.getLadoIzquierdo();
   ladoD= miFicha.getLadoDerecho();
    // if(ladoJ.equalsIgnoreCase("i")){
        // tableroTemporal = ladoI + "|" + ladoD + " " + tableroTemporal ;
        // interfaz.mostrarfichasMesa(tablero);
      // }
      // else{
      // tableroTemporal = tableroTemporal + " "+ ladoI + "|" + ladoD;
      
    
   // return miParteDeLaMesa; }  
return miParteDeLaMesa;
}
}
